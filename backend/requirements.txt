# FIR + CDR + Transactions Relationship Extractor — requirements
# Install with:  pip install -r requirements.txt
#
# After installing, download the spaCy English model separately
# (pip alone does not fetch it):
#   python -m spacy download en_core_web_sm
#
# google-generativeai is only needed if you set USE_GEMINI = True
# in the script. Leave it installed either way — it's harmless if unused.

spacy==3.8.16
google-generativeai>=0.8.0

# After doing the pip install -r requirements.txt, do the 
# pip install -r requirements.txt
# python -m spacy download en_core_web_sm